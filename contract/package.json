{
  "name": "@midnight-private-payroll/contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "compiled/index.js",
  "types": "compiled/index.d.ts",
  "scripts": {
    "compact": "compact compile contracts/payroll.compact compiled",
    "compact:wasm": "compact compile --backend wasm contracts/payroll.compact compiled",
    "build": "tsc --build",
    "test": "node --test"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "^0.19.0"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
