# Midnight Local DevNet Standalone Environment for Private Payroll
# Services:
# - midnight-node: Local Substrate-based consensus & ledger node (Port 9944)
# - indexer: Standalone GraphQL & WebSocket indexer (Port 8088)
# - proof-server: Zero-knowledge transaction proving server (Port 6300)

services:
  midnight-node:
    image: midnightntwrk/midnight-node:0.22.0
    container_name: midnight-node
    ports:
      - "9944:9944"
    environment:
      CFG_PRESET: dev
      CHAIN: dev
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -sf -X POST -H 'Content-Type: application/json' -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"chain_getBlockHash\",\"params\":[1]}' http://localhost:9944 | grep -q '\"result\":\"0x'",
        ]
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 10s
    restart: unless-stopped

  indexer:
    image: midnightntwrk/indexer-standalone:4.0.0
    container_name: midnight-indexer
    ports:
      - "8088:8088"
    environment:
      RUST_LOG: "indexer=info,chain_indexer=info,indexer_api=info,wallet_indexer=info,indexer_common=info,fastrace_opentelemetry=off,info"
      APP__APPLICATION__NETWORK_ID: undeployed
      APP__INFRA__NODE__URL: "ws://midnight-node:9944"
      APP__INFRA__STORAGE__PASSWORD: indexer
      APP__INFRA__PUB_SUB__PASSWORD: indexer
      APP__INFRA__LEDGER_STATE_STORAGE__PASSWORD: indexer
      APP__INFRA__SECRET: "303132333435363738393031323334353637383930313233343536373839303132"
      APP__INFRA__SPO_NODE__URL: "ws://midnight-node:9944"
      APP__INFRA__SPO_NODE__BLOCKFROST_ID: e2e-test-dummy-id
    healthcheck:
      test: ["CMD-SHELL", "cat /var/run/indexer-standalone/running"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s
    depends_on:
      midnight-node:
        condition: service_healthy
    restart: unless-stopped

  proof-server:
    image: midnightntwrk/proof-server:8.0.3
    container_name: midnight-proof-server
    ports:
      - "6300:6300"
    environment:
      - PORT=6300
    command: ["midnight-proof-server", "-v"]
    healthcheck:
      test: ["CMD-SHELL", "echo > /dev/tcp/127.0.0.1/6300"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 5s
    restart: unless-stopped
